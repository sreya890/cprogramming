#include <stdio.h>
#include <stdlib.h>
int main
{
    int b,h,area;
    printf("height  and base of the triangle b,h");
    scanf("%d %d",&b,&h);
    area=0.5*b*h;
    printf("area of the triangle %d %d" base,height};
}
