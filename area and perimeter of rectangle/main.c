#include <stdio.h>

int main()
{int l,b,area,perimeter;
printf("side of the rectangle l,b");
scanf("%d %d",&l,&b);
area=l*b;
perimeter=2*(l+b);
printf("area and perimeter of the rectangle %d %d",area,perimeter);
}
