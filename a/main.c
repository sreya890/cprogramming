#include <stdio.h>
#include <stdlib.h>

int main()
{
  int a[100],i,n;
  printf("enter the array");
  for(i=0;i<n;i++)
  {
      scanf("%d",&a[i]);
      if(a[i]==i)
        break;
  }
  else
    printf("%d",&a[i]);
  }
